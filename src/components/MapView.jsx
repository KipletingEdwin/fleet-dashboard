

export const MapView = ({vehicles}) => {
  return (
        <div className="flex items-center justify-center h-full bg-gray-100 text-gray-500">
      <p>Map or Grid View Placeholder ({vehicles.length} vehicles)</p>
    </div>
  )
}
